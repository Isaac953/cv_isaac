/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  output: 'export',
  basePath: '/cv_isaac',
  images: {
    unoptimized: true,
  },
};

module.exports = nextConfig;
 
module.exports = {
  output: 'export',
}
